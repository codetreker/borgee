name: Publish OpenClaw Plugin

on:
  workflow_dispatch:

permissions:
  contents: write
  id-token: write
  pull-requests: write

jobs:
  publish:
    runs-on: ubuntu-latest
    environment: NPM_TRUSTED_CONN
    permissions:
      id-token: write
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org
      - run: pnpm install --frozen-lockfile --filter @codetreker/borgee-openclaw-plugin
      - run: pnpm --filter @codetreker/borgee-openclaw-plugin build
      - name: Bump version
        working-directory: packages/plugins/openclaw
        run: |
          npm version patch --no-git-tag-version
          VERSION=$(node -p "require('./package.json').version")
          echo "VERSION=$VERSION" >> $GITHUB_ENV
      - name: Publish
        working-directory: packages/plugins/openclaw
        run: npm publish --provenance --access public
      - name: Commit version bump via PR
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          BRANCH="chore/plugin-bump-v${{ env.VERSION }}"
          git checkout -b "$BRANCH"
          git add packages/plugins/openclaw/package.json
          git commit -m "chore(plugin): bump to v${{ env.VERSION }}"
          git push origin "$BRANCH"
          gh pr create --title "chore(plugin): bump to v${{ env.VERSION }}" --body "Auto version bump after npm publish." --base main --head "$BRANCH"
          gh pr merge "$BRANCH" --auto --squash
        env:
          GH_TOKEN: ${{ github.token }}
